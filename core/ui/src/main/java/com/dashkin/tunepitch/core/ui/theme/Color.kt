package com.dashkin.tunepitch.core.ui.theme

import androidx.compose.ui.graphics.Color

// === Neon Palette ===

// Background layers (deep dark)
val NeonBackground = Color(0xFF0A0A1A)
val NeonSurface = Color(0xFF12122A)
val NeonSurfaceVariant = Color(0xFF1A1A3E)
val NeonSurfaceBright = Color(0xFF222252)

// Primary — Electric Cyan
val NeonCyan = Color(0xFF00F0FF)
val NeonCyanDim = Color(0xFF00B8C4)
val NeonCyanMuted = Color(0xFF007A85)

// Secondary — Neon Magenta
val NeonMagenta = Color(0xFFFF00E5)
val NeonMagentaDim = Color(0xFFC400B0)
val NeonMagentaMuted = Color(0xFF8A007C)

// Tertiary — Electric Blue
val NeonBlue = Color(0xFF4D6DFF)
val NeonBlueDim = Color(0xFF3A50CC)

// Accent — Neon Purple
val NeonPurple = Color(0xFFB44DFF)
val NeonPurpleDim = Color(0xFF8A33CC)

// On-colors (text on dark backgrounds)
val NeonTextPrimary = Color(0xFFE8E8FF)
val NeonTextSecondary = Color(0xFFA0A0C0)
val NeonTextTertiary = Color(0xFF6A6A8A)

// Pitch accuracy — neon variants
val PitchGreen = Color(0xFF00FF87)
val PitchYellow = Color(0xFFFFE500)
val PitchRed = Color(0xFFFF3366)

// Difficulty colors — neon variants
val DifficultyBeginner = Color(0xFF00FF87)
val DifficultyIntermediate = Color(0xFFFFAA00)
val DifficultyAdvanced = Color(0xFFFF3366)

// Glow / overlay helpers
val NeonGlowCyan = Color(0x4000F0FF)
val NeonGlowMagenta = Color(0x40FF00E5)
val NeonGlowPurple = Color(0x40B44DFF)
